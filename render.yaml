services:
  # ── Backend Web Service (FastAPI) ───────────────────────────────────────────
  - type: web
    name: finpilot-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - fromGroup: finpilot-shared-secrets
      - key: DATABASE_URL
        fromDatabase:
          name: finpilot-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: finpilot-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: finpilot-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: finpilot-redis
          property: connectionString
      - key: APP_ENV
        value: production
      - key: DEBUG
        value: "false"

  # ── Celery Background Worker ────────────────────────────────────────────────
  - type: worker
    name: finpilot-celery-worker
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    dockerCommand: celery -A app.tasks.celery_app worker --loglevel=info --concurrency=2
    plan: free
    envVars:
      - fromGroup: finpilot-shared-secrets
      - key: DATABASE_URL
        fromDatabase:
          name: finpilot-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: finpilot-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: finpilot-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: finpilot-redis
          property: connectionString
      - key: APP_ENV
        value: production
      - key: DEBUG
        value: "false"

  # ── Redis Service ───────────────────────────────────────────────────────────
  - type: redis
    name: finpilot-redis
    plan: free
    ipAllowList: []

# ── Databases ─────────────────────────────────────────────────────────────────
databases:
  - name: finpilot-postgres
    databaseName: finpilot
    plan: free

# ── Shared Secrets Group ──────────────────────────────────────────────────────
envVarGroups:
  - name: finpilot-shared-secrets
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: ENCRYPTION_KEY
        generateValue: true
